import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs'
import { withThemes } from '@dhruvkb/storybook-addon-themes/dist/vue'

import { ab } from 'tests/fixtures/fs_tree.fix'

import Prompt from '@/components/prompt/Prompt.vue'

<Meta
  title="Internal/Prompt"
  component={Prompt}
  decorators={[
    withThemes,
  ]}/>

# Prompt

<Description of={Prompt}/>

<ArgsTable of={Prompt}/>

The `Prompt` component automatically fetches data from the underlying Vuex store
module.

export const Template = (args) => ({
  components: { Prompt },
  setup() {
    return { args }
  },
  template: `
    <Prompt v-bind="args"/>
  `,
})

<Canvas>
  <Story name="Default">
    {Template.bind({})}
  </Story>
</Canvas>

The working directory can be overridden, which is useful when showing past
interactions in a different working directory.

<Canvas>
  <Story
    name="Overridden"
    args={{
      workingDirectory: ab.toJSON()
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
