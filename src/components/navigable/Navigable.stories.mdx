import { ArgsTable, Canvas, Description, Meta, Story } from '@storybook/addon-docs'
import { withThemes } from '@dhruvkb/storybook-addon-themes/dist/vue'

import { a } from 'tests/fixtures/fs_tree.fix.ts'

import Navigable from '@/components/navigable/Navigable.vue'

<Meta
  title="Elements/Navigable"
  component={Navigable}
  decorators={[
    withThemes,
  ]}/>

# Navigable

<Description of={Navigable}/>

<ArgsTable of={Navigable}/>

export const Template = (args) => ({
  components: { Navigable },
  setup() {
    return { args }
  },
  template: `
    <Navigable v-bind="args"/>
  `,
})

export const defaultArgs = {
  node: a.toJSON(),
  showAliases: true,
  isClickable: true,
}

## Default

In the absence of styling, the `Navigable` component looks like this.

<Canvas>
  <Story
    name="Default"
    args={defaultArgs}>
    {Template.bind({})}
  </Story>
</Canvas>

## Styling

The component can be styled by setting the following CSS custom properties.

| Property         | Description                 | Default                  |
|------------------|-----------------------------|--------------------------|
| `--folder-color` | Text color for folder links | `--color-normal-cyan`    |
| `--file-color`   | Text color for file links   | `--color-normal-magenta` |
| `--alias-color`  | Text color for node aliases | `--color-secondary-fg`   |

Here is `Navigable` with some custom styling applied.

```css
.navigable {
  --folder-color: var(--color-normal-green);
  --file-color: var(--color-normal-blue);
  --alias-color: var(--color-normal-fg);
}
```

<Canvas>
  <Story
    name="Styled"
    args={{
      ...defaultArgs,
      style: {
        '--folder-color': 'var(--color-normal-green)',
        '--file-color': 'var(--color-normal-blue)',
        '--alias-color': 'var(--color-normal-fg)',
      },
    }}>
    {Template.bind({})}
  </Story>
</Canvas>
